{
  # QNix module options for this server
  qnix = {
    # Core modules (always available)
    # sops.enable = true;
    # ssh-server.enable = true;
    # user.enable = true;
    
    # Server modules (available because categories = ["core", "server"])
    # nextcloud.enable = true;
    # Other server services...
    
    # Desktop modules are not available (not in categories)
  };
}

